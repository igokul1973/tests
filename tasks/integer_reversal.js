/**
 * Given an integer, reverse the order of its digits.
 * @param {number} str
 * @returns {number}
 */
function reverse(int) {

}

console.log(reverse(1234) === 4321);
console.log(reverse(-1340) === -431);

/* describe("Integer Reversal", () => {
 it("Should reverse integer", () => {
  assert.equal(reverse(1234), 4321);
  assert.equal(reverse(-1200), -21);
 });
}); */